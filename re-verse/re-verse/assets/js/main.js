// RE-VERSE — main.js
// Theme toggling is driven by settings.php (server-side, stored in session
// so it persists and doesn't require JS). This file just adds small,
// progressive-enhancement touches.

document.addEventListener('DOMContentLoaded', () => {
    // Nothing destructive happens if JS is disabled — the whole site works
    // without it, since navigation is link/form based throughout.
});
